# SPDX-FileCopyrightText: 2019-Present Famedly GmbH
#
# SPDX-License-Identifier: AGPL-3.0-or-later

include: analysis_options.standards.yaml

# managed-by: engineering-standards's `dart-lints-included` pre-commit hook
# requires this file to include `analysis_options.standards.yaml` (generated
# by `nix run .#filegen-activate`, see flake.nix). Repository-specific
# overrides belong here.

analyzer:
  errors:
    # TODO: Enable this lint and fix it
    non_constant_identifier_names: ignore
